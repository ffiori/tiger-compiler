/* valid rec comparisons */
let 
	type rectype = {name : string, id : int}
	var b : rectype := nil
in
	if (b = nil) then printInt(1) else printInt(0);   /* should print 1 */
	if (b <> nil) then printInt(0) else printInt(1);  /* should print 1 */
	0
end
