/* define a recursive function */
let
  /* calculate n! */
  function fact (n : int) : int =
    if  n = 0
      then 1
      else n * fact (n - 1)
in
  printInt(fact (10));0 /* Should print 3628800 */
end
